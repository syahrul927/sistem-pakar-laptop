import { type NextPage } from "next";
import Head from "next/head";
import Header from "~/components/Header";
import { useTheme } from "next-themes";
import { useEffect } from "react";
import ThemeToggle from "~/components/ThemeToggle";
import ArticleThumbnail from "~/components/ArticleThumbnail";

const Home: NextPage = () => {
    const { setTheme } = useTheme();
    useEffect(() => {
        setTheme("light");
    }, [setTheme]);

    return (
        <>
            <Head>
                <title>Take it Easy</title>
                <meta name="description" content="Learning purpose" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <div className="break-words bg-white leading-6 text-zinc-900 transition-colors duration-500 dark:bg-zinc-900 dark:text-zinc-300">
                <Header />
                <main className="flex h-screen w-full items-center justify-center">
                    <div className="grid max-w-xl grid-cols-2 gap-5 p-4 lg:grid-cols-3">
                        <ArticleThumbnail />
                        <ArticleThumbnail />
                        <ArticleThumbnail />
                        <ArticleThumbnail />
                        <ArticleThumbnail />
                    </div>
                </main>
            </div>
        </>
    );
};

export default Home;
